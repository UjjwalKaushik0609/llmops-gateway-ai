# ============================================================
# LLMOps Gateway AI - Requirements
# Tested on: Python 3.12, Windows 11, no C++ Build Tools required
# ============================================================

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# LLM Providers (only Gemini is required to run; others optional)
google-generativeai==0.8.3
openai==1.51.0
anthropic==0.34.2
mistralai==1.1.0

# LangGraph (multi-agent orchestration) - LangChain trimmed to avoid
# heavy/slow dependency resolution; LangGraph alone is sufficient for
# the agent pipeline used in this project.
langgraph==0.2.28
langchain-core==0.3.15

# Database
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Cache (uses redis.asyncio — do NOT install aioredis, it is broken on Python 3.11+)
redis==5.1.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12
cryptography==43.0.1

# Observability
prometheus-client==0.21.0
structlog==24.4.0
rich==13.9.2

# Utilities
httpx==0.27.2
tenacity>=8.1.0,<9.0.0
tiktoken==0.8.0
python-dotenv==1.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# ============================================================
# OPTIONAL — Vector memory / RAG (commented out by default)
# Requires Microsoft C++ Build Tools to compile on Windows.
# Uncomment only if you have installed:
# https://visualstudio.microsoft.com/visual-cpp-build-tools/
# ============================================================
# chromadb==0.5.23
# sentence-transformers==3.1.1

# ============================================================
# OPTIONAL — Dev tooling (not required to run the app)
# ============================================================
# black==24.10.0
# ruff==0.6.9
# mypy==1.11.2
# pre-commit==4.0.0
